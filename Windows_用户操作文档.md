# 视频控制台 Windows 用户操作文档

## 1. 适用范围

本文档用于指导 Windows 用户安装和使用“视频控制台”程序。

程序功能包括：

- 打开课程视频页面。
- 自动登录账号。
- 自动检测并播放视频。
- 自动切换二倍速。
- 当前视频完成后自动播放下一个视频。
- 当前章节完成后自动进入下一节。
- 支持暂停、停止、导出日志。
- 支持 Bark 手机推送提醒。

## 2. 运行要求

使用电脑需要满足：

- Windows 10 或 Windows 11。
- 已安装 Microsoft Edge 或 Google Chrome。
- 能正常访问课程网站。

说明：

- Windows 一般自带 Microsoft Edge，默认选择 Edge 即可。
- 当前安装包不会内置 Chromium，也不会额外下载 Chromium。

## 3. 安装程序

拿到压缩包后，先解压：

```text
VideoMonitorConsole-Windows.zip
```

解压后通常会看到：

```text
VideoMonitorConsole.exe
VideoMonitorConsoleSetup.exe
README_WINDOWS.md
```

推荐使用安装包：

```text
VideoMonitorConsoleSetup.exe
```

双击 `VideoMonitorConsoleSetup.exe`，按照安装向导点击 `Next` / `Install` 完成安装。

安装完成后，可以从开始菜单打开：

```text
Video Monitor Console
```

也可以直接双击运行：

```text
VideoMonitorConsole.exe
```

## 4. 启动程序

启动后会出现一个黑色命令行窗口，并自动打开浏览器控制台页面。

默认地址一般是：

```text
http://127.0.0.1:8000/
```

如果 8000 端口不可用，程序会自动切换到其他端口。请以黑色命令行窗口里显示的地址为准，例如：

```text
Port 8000 is unavailable; using port 8001.
Video Monitor Console: http://127.0.0.1:8001/
```

使用过程中不要关闭黑色命令行窗口。关闭该窗口后，本地控制台服务会停止。

## 5. 页面配置说明

打开控制台页面后，主要填写以下内容。

### 5.1 课程 URL

填写需要播放的视频课程链接。

注意：

- 请复制完整链接。
- 链接中包含 `&` 是正常的，直接粘贴到网页输入框即可。

### 5.2 手机号和密码

如果课程页面每次都需要登录，请填写：

```text
手机号
密码
```

并保持勾选：

```text
自动登录
```

如果不需要保存密码，不要勾选：

```text
记住密码
```

### 5.3 浏览器

可选择：

```text
Edge
Chrome
```

推荐默认使用：

```text
Edge
```

如果 Edge 无法打开，再切换为 Chrome。

### 5.4 播放倍速

默认值：

```text
2
```

表示自动切换为二倍速播放。

### 5.5 下一节选择器

默认值：

```text
#prevNextFocusNext
```

通常不需要修改。

程序会在当前页面视频播放完成后，尝试点击“下一节”按钮。

### 5.6 Bark 推送

如果需要手机提醒，可以填写 Bark Key 或完整 Bark URL。

支持两个提醒选项：

```text
每节完成提醒
全部完成提醒
```

填写后建议先点击：

```text
测试推送
```

确认手机能收到通知后再开始运行。

## 6. 开始运行

确认配置后，点击：

```text
开始
```

程序会自动：

1. 打开课程链接。
2. 检测登录页并自动登录。
3. 检测页面中的视频。
4. 将视频切换到设定倍速。
5. 自动播放视频。
6. 当前视频完成后继续播放后续视频。
7. 当前页面完成后尝试进入下一节。

## 7. 暂停、继续和停止

运行中可以使用：

```text
暂停
```

暂停当前视频和自动化动作。

暂停后按钮会变为：

```text
继续
```

点击后继续运行。

如果需要完全结束任务，点击：

```text
停止
```

## 8. 日志和导出

右侧日志区域会显示运行状态，包括：

- 是否检测到视频。
- 视频播放状态。
- 当前播放进度。
- 视频倍速。
- 是否点击下一节。
- Bark 推送是否发送。

如果需要保存日志，点击：

```text
导出
```

如果日志太多，可以点击：

```text
清空
```

## 9. 保存配置

填写好常用配置后，可以点击：

```text
保存配置
```

下次打开控制台页面会自动恢复配置。

注意：

- 密码默认不会保存。
- 只有勾选“记住密码”后，密码才会保存在当前浏览器本地。
- 保存内容只保存在当前电脑的当前浏览器中。

## 10. 常见问题

### 10.1 浏览器打不开 `127.0.0.1:8000`

先看黑色命令行窗口里的实际地址。

如果显示类似：

```text
Port 8000 is unavailable; using port 8001.
Video Monitor Console: http://127.0.0.1:8001/
```

请打开：

```text
http://127.0.0.1:8001/
```

### 10.2 黑色命令行窗口报端口错误

新版程序会自动切换端口。请下载并安装最新构建包。

如果仍然失败，可以重启电脑后再运行。

### 10.3 点开始后没有反应

请检查：

- 课程 URL 是否完整。
- 自动登录开启时，手机号和密码是否已填写。
- 右侧日志是否有错误提示。
- Edge 或 Chrome 是否能正常打开网页。

### 10.4 自动登录失败

可以手动在打开的浏览器页面中登录一次。

如果页面结构变化，自动登录可能需要调整登录输入框选择器。

### 10.5 视频没有自动播放

请检查：

- 是否勾选“自动播放”。
- 是否勾选“静音播放”。
- 浏览器是否弹出权限提示。
- 页面是否真的存在视频。

### 10.6 没有进入下一节

请检查：

- 是否勾选“顺序播放”。
- 当前页面视频是否已经播放完成。
- “下一节选择器”是否仍为 `#prevNextFocusNext`。
- 页面上是否真的存在“下一节”按钮。

### 10.7 Bark 没有推送

请检查：

- Bark Key 或 URL 是否填写正确。
- 是否勾选对应提醒选项。
- 是否点击“测试推送”并收到测试通知。
- 手机 Bark App 是否允许通知。

### 10.8 Windows 提示未知发布者

这是因为程序没有购买代码签名证书。

如果程序来源可信，可以选择继续运行。

## 11. 退出程序

推荐操作：

1. 在网页控制台点击“停止”。
2. 关闭浏览器控制台页面。
3. 关闭黑色命令行窗口。

关闭黑色命令行窗口后，程序会完全停止。

## 12. 使用注意事项

- 使用时请确保你拥有对应课程内容的播放和使用权限。
- 请遵守目标网站或平台的使用条款。
- 不要把包含个人账号、密码、Bark Key 的截图或日志发给他人。
